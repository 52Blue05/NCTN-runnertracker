# ===========================
# Runner Tracker — Profile Selector
# ===========================
# Local dev: spring.profiles.active=dev  (mặc định)
# Production: chạy với --spring.profiles.active=prod hoặc set biến môi trường
# ===========================
spring.profiles.active=dev
